export const BRAND = {
  /** The main title, in the navbar. */
  logo: "anujsingh.notes",
  /** The two lines of the out-of-focus intro label. */
  introLine1: "Hey there, meet A.R.I.A,",
  introLine2: "the Adaptive Response Interface Agent at anujsingh.notes",
  /**
   * Address on the outline pill, and what the copy icon puts on the
   * clipboard. The spec's `hello@mainframe.co` belonged to the old name,
   * and `.notes` is not a real TLD, so an address at the new one would
   * not deliver. This is the address the rest of the site already uses.
   */
  email: "iamasr@duck.com",
} as const;
